<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<select id="selectAll" resultType="List">
		select * from member
	</select>
	<select id="selectEmail" parameterType="Map"
		resultType="com.finotek.noticeboard.vo.MemberVo">
		select email, pass, name from member
		where email=#{email} and pass=#{pass}
	</select>
	
	<insert id="insert">
		insert into member(email, pass, name)
		values(#{email}, #{pass}, #{name})
	</insert>
	
	<update id="update">
		update member set
		pass=#{pass},
		name=#{name}
		where email=#{email}
	</update>
</mapper>